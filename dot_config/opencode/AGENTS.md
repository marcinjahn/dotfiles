# General

- when you are given some task, use appropriate subagent proactively.
- don't use git commands. jj tool is used instead of git. However, do not use it, rather just focus
  on the code without worrying about VCS.
- prefer to use LSP when looking for specific code symbols
- use gh CLI to access GitHub resources (code, PRs, etc.)
